{
  "name": "paperplane-gift-delivery-tracker",
  "version": "1.0.0",
  "description": "Gift Delivery Route & Dispatch Tracker for Paper Plane",
  "scripts": {
    "install-all": "npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "backend": "npm.cmd start --prefix backend",
    "frontend": "npm.cmd run dev --prefix frontend",
    "dev": "npx.cmd concurrently \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\"",
    "seed": "npm.cmd run seed --prefix backend"
  }
}
